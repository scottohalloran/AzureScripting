#!/bin/bash
#Deletes the resource group ContosoResourceGroup
#This will also delete all the resources within the resource group

# Delete resource group
az group delete --name ContosoResourceGroup --yes


